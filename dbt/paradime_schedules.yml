schedules:
  - name: hourly
    schedule: "15 * * * *" 
    environment: production
    commands:
         - dbt seed
         - dbt run
    owner_email: giancarlo@levity.ai 
    slack_notify:
         - "#production-environment-paradime-errors"
         - "owner"