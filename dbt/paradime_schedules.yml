schedules:
  - name: hourly
    schedule: "@hourly"
    environment: production
    commands:
         - dbt seed
         - dbt run
    owner_email: giancarlo@levity.ai 
    slack_notify:
         - "#production-environment-paradime-errors"
         - "owner"